@startuml
title UC-03 — Full Outfit Recommendation (Activity)
start
:User selects context\n(occasion + style intent);
:User optionally provides weather label\n(manual or adapter);
:System loads wardrobe inventory;
:System builds candidate pool\n(per category);
:Generate outfit combinations\n(top/bottom/shoes...);
:Score outfits\n(fuzzy rules + context);
:Rank top-N outfits;
:Display recommendations\n+ explanations;
if (User selects outfit?) then (yes)
  :Store selection;
  :Ask for feedback;
  if (Feedback given?) then (yes)
    :Store feedback locally;
    :Update personalization weights\n(optional);
  endif
else (no)
  :User tweaks context\nor exits;
endif
stop
@enduml
